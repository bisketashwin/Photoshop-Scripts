// Author Ashwin Nandihalli#target photoshopvar doc = activeDocument;  var layerList = new Array()  var extOld = "Event076_";var extNew = "Event095_";var longNameOld = "Event076_";var longNameNew = "Event095_"; // replace with nothingvar cLayerloopLayers(doc);  function replaceText(cLayer, search, replacement ){   cLayer.name = cLayer.name.replace(new RegExp(search, 'g'), replacement);}  function changename(cLayer) {     if(cLayer.name){          if(cLayer.name.indexOf (extOld) > -1 )     {           replaceText(cLayer, extOld, extNew);         }     if(cLayer.name.indexOf (extOld.toUpperCase()) > -1)     {           replaceText(cLayer, extOld.toUpperCase(), extNew.toUpperCase());         }     if (cLayer.name.indexOf (longNameOld) >-1)     {            replaceText(cLayer, longNameOld, longNameNew );      }  }}function loopLayers (theParent, allLayers) {            if (!allLayers) {var allLayers = new Array}             else {};            for (var m = theParent.layers.length - 1; m >= 0;m--) {                      var theLayer = theParent.layers[m];  // apply the function to layersets;                      if (theLayer.typename == "ArtLayer") {                                allLayers.push(theLayer)                                changename(theLayer)                              }                      else {                          changename(theLayer)                              allLayers = (loopLayers(theLayer, allLayers))  // this line includes the layer groups;                                allLayers.push(theLayer);                                }                      };            return allLayers  }; 